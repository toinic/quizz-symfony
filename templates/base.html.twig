<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Quizz | {% block title %}{% endblock %}</title>

    <link href="{{asset('css/bootstrap.min.css')}}" rel="stylesheet">
    <link href="{{asset('css/styles.css')}}" rel="stylesheet">
    {% block stylesheets %}{% endblock %}
  </head>
  <body class="text-center d-flex w-100 h-100 mx-auto flex-column">

    <header class="sticky-top mb-auto">
      <div class="bg-dark text-light">
        <div class="d-flex flex-row justify-content-between align-items-center">
            <a href="/" class="navbar-brand">
              <span id="myLogo">AL</span>
              Symfony Quizz
            </a>
            <nav class="navbar navbar-expand-sm navbar-dark">
              <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                  <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link" href="{{path('question')}}">Questions</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link" href="{{path('answer')}}">Answers</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link" href="{{path('category')}}">Categories</a>
                  </li>
                </ul>
              </div>
            </nav>
        </div>
      </div>
    </header>

    <main class="mt-4 mb-4">
      {% block body %}{% endblock %}
    </main>

    <footer class="mt-auto">
      <div class="d-flex flex-row justify-content-center align-items-center bg-dark p-5">
        <span>Application made with <a href="https://symfony.com/">Symfony</a></span>
        <span>, by <a href="https://twitter.com/anlafon">@Antoine Lafon</a>.</span>
      </div>
    </footer>

    <script type="text/javascript" src="{{asset('js/jquery-3.3.1.min.js')}}"></script>
    <script type="text/javascript" src="{{asset('js/bootstrap.min.js')}}"></script>
    <script type="text/javascript" src="{{asset('js/app.js')}}"></script>
    {% block javascripts %}{% endblock %}

  </body>
</html>
